<h1>Řešení úloh roc<%=@annual%>se<%=@round%><%if @problem_no%>ul<%=@problem_no%><%end%></h1>

<table class="table table-bordered">
  <tr> 
   <th>Id</td>
   <th>Se</td>
   <th>Úl</td>
   <th>Soubor</td>
   <th>Řešitel</td>
   <th>Orig</td>
   <th>Body</td>
 </tr> 
<% @solutions.each do |sol| %>
  <tr>
    <td><%= link_to sol.id, :action => :show, :id => sol.id %></a><t/d>
    <td><%= sol.sosna_problem.round %></td>
    <td><%= sol.sosna_problem.problem_no %></td>
    <td>
      <% if sol.filename  %>
         <%= link_to sol.filename , {:action => :download, :id => sol.id}, {:title => sol.orig_filename} %></td> 
      <% else %>
         <i>žádný soubor</i>
      <% end %>
    <td><%= sol.sosna_solver.full_name%></td>
    <td><%= sol.orig_filename %></td>
    <td><%= sol.score || '-'%> </td>
  </tr>
<% end %>
</table>

<%= link_to 'Stáhnout všechna řešení v jednom .zip',
            { :action => :downall, :roc => @annual, :se => @round, :ul => @problem_no },
            {:class=> 'btn btn-primary'} %>


<hr>
annuals: <%= debug @annuals %>
rounds: <%= debug @rounds %>
problems: <%= debug @problems %>
