<div class="col-xs-12">
<div class="act_events_bck_white">
  <h1>Zobrazování akcí</h1>
</div>

<p class="act_events_bck_white">
Řešíš Pikomat a chtěl bys poznat ostatní řešitele? Máš pocit, že se ve třídě nemáš s kým bavit, a rád bys někam zapadl? Přijď na některé z našich setkání! Pravidelně pořádáme jednodenní i vícedenní akce, kterých se účastní jak naši nejlepší řešitelé, tak i ti, kteří s řešením zrovna začínají. Pikomat, to je kolektiv stmelený dohromady společnými prožitky a společenství plné přátelské atmosféry.
</p>

<% unless Act::Participant::has_participant?(current_user) %>
  <p>
    <div class="alert alert-warning">
      Abys ses mohl přihlásit na nějakou akci, musíš mít založený účastnický účet, a navíc ještě musíš být přihlášený/á. Účastnický účet si můžeš založit
      <%= link_to "zde", act_participant_new_path %>.
    </div>
  </p>
<% end %>

<% if can? :new, Act::Event %>
  <a href=<%= act_event_new_url %> class="btn btn-primary" type="submit">
    <span class="ace-icon fa fa-edit"></span>
    <span>Vytvořit </span>
    <span class="hidden-xs">novou </span>
    <span>akci</span>
  </a>
<% end %>
<% if can? :read, Act::EventCategory %>
  <a href=<%= act_event_categories_url %> class="btn btn-primary" type="submit">
    <span class="ace-icon fa fa-flask"></span>
     <span>Typy akcí</span>
  </a>
<% end %>
<% if current_user && current_user.org? %>
  <a href=<%= act_events_jakna_path %> class="btn btn-primary" type="submit">
    <span class="ace-icon fa fa-print"></span>
    <span>Návod</span>
    <span class="hidden-xs"> na používání</span>
  </a>
<% end %>

<% if (can? :new, Act::Event) || (can? :read, Act::EventCategory) || (current_user && current_user.org?) %>
  <br><br>
<% end %>

<div class="tabbable">
  <ul id="myTab" class="nav nav-tabs">
    <% if Act::Participant::has_participant?(current_user) %>
      <% Act::Event::FILTER_STATUSES.each do |status| %>
        <li <%= class_active_if status[:code] == @enroll_status %> >
        <%= link_to status[:text], act_events_filter_path(event_category: @event_category, enroll_status: status[:code]) %></li>
      <% end %><br><br>
    <% end %>

    <% Act::Event::category_filter_all(current_user).each do |category| %>
      <li <%= class_active_if category[:code] == @event_category %> >
      <%= link_to category[:text], act_events_filter_path(event_category: category[:code], enroll_status: @enroll_status) %></li>
    <% end %>
  </ul>

  <div class="tab-content">
    <div class="tabtab-pane in active">
      <div class="table-div">
        <table class="table table-hover table-condensed table-bordered">
        <%= render :partial => "events_brief", locals: { events: @future_events, half_page: false, old: false } %>
        <% unless (@future_events.empty? || @past_events.empty?) %>
          <tr class= "act_borderless"><td colspan="4" style="border-left:1px solid #FFFFFF;border-right:1px solid #FFFFFF">&nbsp;</td></tr>
        <% end %>
        <%= render :partial => "events_brief", locals: { events: @past_events, half_page: false, old: true} %>
        </table>
      </div>
    </div>
  </div>
</div>
</div>
