%id;Příjmení;Jméno;Škola;Datum;<% @problem_numbers.each do |no| %>u<%=no%>;<% end %><% @problem_numbers.each do |no| %>B<%=no%>;<% end %>
<% @solvers.each do |solver| 
  %><%= solver.id 
  %>;<%= solver.last_name
  %>;<%= solver.name
  %>;<%= solver.school.nil? ? '' : solver.school.short
  %>;;<% @problem_numbers.each do |no| 
   %><% sol_arr = @solutions_by_solver[solver.id]  
   %><% sol  =  sol_arr.nil?  ? nil : sol_arr[no]  
   %><% if !sol.nil? && sol.filename
          %>A<%
        else
          %><%
        end 
   %>;<%
 end %>
<% end %>
