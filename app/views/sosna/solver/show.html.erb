
<h1>Editace řešitele</h1>

<%= render partial: 'form', locals: { is_edit: true } %>


<hr>

<% if can? :delete, Sosna::Solver %>
  <%= form_tag sosna_solver_delete_path(id:@solver.id) do %>
    <%= submit_tag  'Smazat uživatele (nebude žádné "jsi si jistý?")', :class=>'btn btn-danger' %></div>
  <% end %>
<% else %>
    <%= submit_tag  'Smazat uživatele může adm', :class=>'btn disabled' %></div>
<% end %>
