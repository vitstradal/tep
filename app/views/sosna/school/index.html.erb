
<h1>Seznam škol</h1>
<table class="table table-bordered datatable">
 <thead>
   <tr>
    <th>Id</th>
    <th>Zkr.</th>
    <th title="Univerzal Id">UniID</th>
    <th>Jméno</th>
    <th title="Papír">P</th>
    <th>Adresa</th>
   </tr>
 </thead>
 <tbody>
  <% @schools.each do |sch| %>
    <tr>
      <td><%= link_to sch.id, :action => :show, :id => sch.id %></a></td>
      <% cls = @shorts[sch.short] > 1 ? 'bg-red' : nil %>
      <td <%if cls%>class="<%=cls%>"<%end%>><%= sch.short%></td>
      <td><%= sch.universal_id %> </td>
      <td><%= sch.name %></td>
      <td><%= sch.want_paper ? 'Y' : ' ' %></td>
      <td><%= sch.address %> </td>
    </tr>
  <% end %>
 <tbody>
</table>


<%= link_to 'Nová škola', :sosna_school_new, :class => 'btn btn-success' %>
<%= link_to 'skoly.pik', sosna_schools_url(format: 'pik'), {:class => 'btn btn-primary'}  %>
